<!DOCTYPE html>
<html>
  <head>
    <title>Review</title>
    <%= csrf_meta_tags %>
    <link href="//netdna.bootstrapcdn.com/bootstrap/3.1.0/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="header">
    <nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <div class="title cleafix">
            <a class="navbar-brand" href="/citicizes">BEST review</a>
          </div>
          <div id="gnavi" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
              <% if user_signed_in? %>
              <li>
                <%= link_to user_path(current_user.id), :class => "mypage-link-icon" do%>
                  <span class="glyphicon glyphicon-user"></span>
                <% end %>
              </li>
              <% end %>
              <li>
                <a href="/citicizes">一覧</a>
              </li>
              <li>
                <a href="/citicizes/new">投稿する</a>
              </li>
              <% if user_signed_in? %>
                <li>
                  <%= link_to destroy_user_session_path, method: :delete, data: { confirm: 'ログアウトしますか？'} do %>
                    <span>ログアウト</span>
                  <% end %>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </nav>
  </div>

    <%= yield %>
  </body>
</html>
